//the number which you want the root of
const number = prompt("type the number in here :) ");

// using math.sqrt to find the root
const result = Math.sqrt(number);

console.log(`the root of ${number} is ${result}`);
